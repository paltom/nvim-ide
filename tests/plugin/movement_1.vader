Before (Setup windows layout - 3x3 grid):
  split|split
  vsplit|vsplit
  execute "normal! \<c-w>j\<cr>"
  vsplit|vsplit
  execute "normal! \<c-w>j\<cr>"
  vsplit|vsplit
  execute "normal! 5\<c-w>w" 
  AssertEqual 5, winnr()

After (Cleanup windows):
  only
  AssertEqual 1, winnr("$")

Do (Normal mode - move up):
  \<a-k>

Then (In above window):
  AssertEqual 2, winnr()

Do (Normal mode - move down):
  \<a-j>

Then (In below window):
  AssertEqual 8, winnr()

Do (Normal mode - move left):
  \<a-h>

Then (In left window):
  AssertEqual 4, winnr()

Do (Normal mode - move right):
  \<a-l>

Then (In right window):
  AssertEqual 6, winnr()

Do (Insert mode - move down):
  i
  \<a-j>

Then (In below window): 
  AssertEqual 8, winnr()

Do (Insert mode - move up):
  i
  \<a-k>

Then (In above window): 
  AssertEqual 2, winnr()

Do (Insert mode - move right):
  i
  \<a-l>

Then (In right window): 
  AssertEqual 6, winnr()

Do (Insert mode - move left):
  i
  \<a-h>

Then (In left window): 
  AssertEqual 4, winnr()

Do (Visual mode - move down):
  v
  \<a-j>

Then (In below window): 
  AssertEqual 8, winnr()

Do (Visual mode - move up):
  v
  \<a-k>

Then (In above window): 
  AssertEqual 2, winnr()

Do (Visual mode - move right):
  v
  \<a-l>

Then (In right window): 
  AssertEqual 6, winnr()

Do (Visual mode - move left):
  v
  \<a-h>

Then (In left window): 
  AssertEqual 4, winnr()

# Terminal mode tests are very hard to make properly - skipping them
